services:
  sf.server:
    image: sf.server
    build:
      context: .
      dockerfile: sf.Server/Dockerfile
    ports:
      - "5056:8080"
      - "5057:8081"
  sf.client:
    image: sf.client
    build:
      context: ./sf.client
      dockerfile: Dockerfile
    ports:
      - "5058:80"